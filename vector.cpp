#include<bitset>/stdc++.h>
using namespace std;

int main(){
    int n;
    cin>>n;
    vector<int>arr(n,0);
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    int x=10;
    arr.push_back(x);

    sort(arr.begin(),arr.end());

    int size=arr.size();



    vector<int>v;
    int x,y,z;
    return 0;
}